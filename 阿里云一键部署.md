# 阿里云一键部署

本文档用于将当前项目直接部署到阿里云 ECS 服务器，并达到与本地当前环境一致的效果：

- 使用 `Dockerfile.local` 构建镜像
- 容器内预装 Playwright WebKit 与系统依赖
- Web 登录账号默认 `zhangbo / zhangbo`
- 默认连接宿主机 MySQL：`127.0.0.1:3306`
- 服务端口：`8001`

对应部署文件：

- Compose 文件：[compose/docker-compose.aliyun.yaml](/Users/zhangbo/work/INKE/study/we-mp-rss/compose/docker-compose.aliyun.yaml)
- 环境变量示例：[.env.aliyun.example](/Users/zhangbo/work/INKE/study/we-mp-rss/.env.aliyun.example)

## 部署前提

服务器需要具备：

- 已安装 Git
- 已安装 Docker 与 Docker Compose Plugin
- 已启动 MySQL，且数据库可从宿主机 `127.0.0.1:3306` 访问
- 阿里云安全组已放行 `8001/TCP`

如果尚未安装 Docker，可先执行：

```bash
curl -fsSL https://get.docker.com | sh
systemctl enable docker
systemctl start docker
docker version
docker compose version
```

## 一键部署命令

以下命令可直接在阿里云服务器执行。

```bash
mkdir -p /opt
cd /opt

git clone git@github.com:zhangbo319/we-mp-rss.git
cd we-mp-rss

mkdir -p data
cp .env.aliyun.example .env.aliyun

docker compose --env-file .env.aliyun -f compose/docker-compose.aliyun.yaml build --no-cache backend
docker compose --env-file .env.aliyun -f compose/docker-compose.aliyun.yaml up -d
docker compose -f compose/docker-compose.aliyun.yaml ps
```

## 部署后验证命令

```bash
cd /opt/we-mp-rss

docker compose -f compose/docker-compose.aliyun.yaml ps
docker logs --tail 100 we-mp-rss

curl http://127.0.0.1:8001/api/docs
curl -X POST http://127.0.0.1:8001/api/v1/wx/auth/login \
  -H 'Content-Type: application/x-www-form-urlencoded' \
  -d 'username=zhangbo&password=zhangbo'
```

如果上面两条 `curl` 都正常返回，说明部署已经成功。

## 访问地址

假设你的阿里云 ECS 公网 IP 为 `<ECS_PUBLIC_IP>`：

- 管理端：`http://<ECS_PUBLIC_IP>:8001/`
- API 文档：`http://<ECS_PUBLIC_IP>:8001/api/docs`
- 登录账号：`zhangbo`
- 登录密码：`zhangbo`

## MySQL 配置说明

默认 `.env.aliyun` 配置如下：

```bash
DB=mysql+pymysql://zhangbo:zhangbo@127.0.0.1:3306/we_mp_rss?charset=utf8mb4
```

这表示：

- MySQL 运行在同一台 ECS 主机
- 宿主机端口为 `3306`
- 数据库名为 `we_mp_rss`
- 用户名密码为 `zhangbo / zhangbo`

如果你使用阿里云 RDS 或外部 MySQL，只需要修改 `.env.aliyun` 中的 `DB=`：

```bash
DB=mysql+pymysql://<用户名>:<密码>@<MySQL主机>:3306/we_mp_rss?charset=utf8mb4
```

## 更新命令

后续更新服务时，直接执行：

```bash
cd /opt/we-mp-rss
git pull
docker compose --env-file .env.aliyun -f compose/docker-compose.aliyun.yaml build backend
docker compose --env-file .env.aliyun -f compose/docker-compose.aliyun.yaml up -d
docker compose -f compose/docker-compose.aliyun.yaml ps
```

## 常用运维命令

查看日志：

```bash
docker logs -f we-mp-rss
```

重启服务：

```bash
cd /opt/we-mp-rss
docker compose --env-file .env.aliyun -f compose/docker-compose.aliyun.yaml restart backend
```

停止服务：

```bash
cd /opt/we-mp-rss
docker compose --env-file .env.aliyun -f compose/docker-compose.aliyun.yaml down
```

## 推荐上线检查项

- 确认阿里云安全组已放行 `8001`
- 确认 ECS 本机防火墙没有拦截 `8001`
- 确认 MySQL 用户允许本机连接
- 首次上线后进入“订阅管理 -> 刷新授权”确认二维码可正常生成
- 进入“订阅管理 -> 添加精选文章”确认 Playwright 抓取链路可正常工作
