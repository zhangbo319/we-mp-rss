# 开发计划-阿里云一键部署与远程推送
创建时间：2026-03-29
当前状态：已完成
执行目标：为当前项目补齐适用于阿里云 Ubuntu 宿主机 MySQL 的 Docker 部署资产，支持通过 80 端口访问控制台，并将本次调整推送到用户 fork 的远端分支。
步骤：[x] 梳理现有 Docker 部署方式与远端状态  [x] 新增阿里云部署 compose 与示例环境变量  [x] 编写阿里云一键部署文档  [x] 校验部署文件与命令  [x] 调整远端并推送分支  [x] 回填结果  [x] 复查阿里云 Linux 宿主机 MySQL 连接方式  [x] 调整 compose 为 host network + 80 端口  [x] 更新部署文档与环境变量示例  [x] 再次校验并提交推送  [x] 回填最终结果
改动明细：
- 已确认当前本地部署依赖 `Dockerfile.local` 与 `compose/docker-compose.host-mysql.yaml`。
- 已确认当前分支为 `main`，且 `origin` 仍指向上游仓库 `git@github.com:rachelos/we-mp-rss.git`。
- 新增阿里云部署文件 `compose/docker-compose.aliyun.yaml`。
- 新增环境变量示例 `.env.aliyun.example`。
- 新增部署文档 `阿里云一键部署.md`，提供首装、更新、验证与运维命令。
- 将 `Dockerfile.local` 的 Debian 源切换为清华镜像并增加 `apt-get` 重试，便于后续在阿里云构建 Playwright 依赖。
- 调整 git 远端：当前 `origin` 指向 `git@github.com:zhangbo319/we-mp-rss.git`，`upstream` 指向 `git@github.com:rachelos/we-mp-rss.git`。
- 新建并推送分支 `codex/2026-03-29-aliyun-deploy`。
- 复查后确认阿里云 Ubuntu 场景下，如果容器走 bridge 网络并访问 `host.docker.internal`，宿主机 MySQL 仍可能受 `127.0.0.1` 监听限制影响。
- 因此本次阿里云方案切换为 `network_mode: host`，应用默认监听宿主机 `80` 端口，并继续使用 `127.0.0.1:3306` 连接宿主机 MySQL。
- 同步更新 `.env.aliyun.example` 与 `阿里云一键部署.md`，补充 MySQL 初始化命令、分支切换命令和 80 端口运维说明。
校验结果：
- `git branch --show-current` 输出 `main`。
- `git remote -v` 显示当前 `origin` 为上游仓库，尚未切换到用户 fork。
- `docker compose --env-file .env.aliyun.example -f compose/docker-compose.aliyun.yaml config` 解析成功。
- `git status --short` 在提交前为空，确认工作区已收敛。
- 提交记录：`c79f77b7 feat: add aliyun deployment assets`。
- `git push -u origin codex/2026-03-29-aliyun-deploy` 成功，远端分支已建立并开始跟踪。
- 本次调整后再次执行 `docker compose --env-file .env.aliyun.example -f compose/docker-compose.aliyun.yaml config`，输出显示 `PORT=80`、`DB=...@127.0.0.1:3306/...`、`network_mode: host`。
- 本次提交记录：`9c3501b7 chore: adjust aliyun docker deployment`。
- `git rev-list --left-right --count HEAD...upstream/main` 输出 `2 0`，确认当前工作分支无需额外合并上游。
- `git push origin codex/2026-03-29-aliyun-deploy` 成功，远端分支已同步最新阿里云部署调整。
