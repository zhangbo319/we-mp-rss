# WeRSS 项目技术架构与使用手册

## 1. 文档目的

本文档基于当前仓库代码、配置样例和已有说明文档整理，目标是帮助你快速理解这个项目的：

- 产品定位
- 技术架构体系
- 目录与模块职责
- 部署与启动方式
- 日常使用流程
- 二次开发与排障入口

本文档主要参考了以下上下文：

- `AGENTS.md`
- `README.zh-CN.md`
- `main.py`
- `web.py`
- `config.example.yaml`
- `core/`、`apis/`、`jobs/`、`driver/`、`web_ui/`
- `docs/CASCADE_QUICKSTART.md`
- `docs/AK_Authentication_Guide.md`

## 2. 项目是什么

WeRSS 是一个“微信公众号订阅助手”。它的核心目标是：

- 通过浏览器自动化能力完成微信公众号后台授权
- 采集公众号文章与元数据
- 将采集结果存储到数据库
- 对外生成 RSS/Feed 链接
- 提供 Web 管理后台、API、Webhook、导出能力
- 支持定时任务与父子节点级联扩展

从产品形态看，它并不是单纯的 RSS 生成器，而是一个“采集 + 管理 + 分发”的完整服务。

## 3. 技术架构总览

### 3.1 架构分层

项目采用前后端分离，但生产环境通常由后端统一托管前端静态资源：

1. 表现层  
   `web_ui/` 中的 Vue 3 应用提供管理后台界面，打包后的静态文件放到 `static/`，由 FastAPI 提供访问。

2. API 与页面入口层  
   `web.py` 创建 FastAPI 应用，挂载 API 路由、静态资源和前端入口页面。

3. 业务服务层  
   `apis/` 负责接口定义，`core/` 负责配置、认证、数据库、RSS 生成、通知、缓存、调度封装等核心能力。

4. 采集与驱动层  
   `driver/` 负责微信授权、Cookie/Token 管理、Playwright 浏览器控制、文章抓取与反爬处理。

5. 任务调度层  
   `jobs/` 负责定时抓取、Webhook 推送、文章补采、级联同步与任务分发。

6. 存储层  
   通过 SQLAlchemy 连接 SQLite/MySQL/PostgreSQL；Redis 为可选增强能力，主要用于缓存、状态与环境异常统计。

### 3.2 核心运行链路

一次完整的业务链路可以理解为：

1. 管理员登录后台
2. 通过扫码方式授权微信公众号后台
3. 系统保存 Cookie、Token 与公众号基础信息
4. 用户添加订阅或创建消息任务
5. 调度器按 Cron 表达式触发采集
6. `driver/` 和 `core/wx/` 拉取公众号文章
7. `core/db.py` 将文章、公众号、任务信息写入数据库
8. `core/rss.py` 生成 RSS/Feed 输出
9. `jobs/webhook.py` 按任务配置把文章推送到外部系统
10. 前端管理台和开放接口同时消费这些数据

### 3.3 入口文件关系

#### 后端启动入口：`main.py`

`main.py` 是整个服务的总装配入口，主要负责：

- 读取 `config.yaml`
- 按参数执行初始化流程 `-init True`
- 启动授权定时服务 `driver/auth.py`
- 按配置启动级联同步或级联网关调度
- 启动普通消息任务调度
- 启动自动补采文章任务
- 通过 Uvicorn 启动 `web:app`

常用启动命令：

```bash
python main.py -job True -init True
```

#### Web 应用入口：`web.py`

`web.py` 负责：

- 创建 FastAPI 实例
- 注册 CORS 中间件
- 注册 AK/SK 认证中间件
- 汇总所有 API Router
- 挂载 `/assets`、`/static`、`/files`
- 兜底把非 API 请求转发到前端 `static/index.html`

这意味着生产环境下通常只需要启动 Python 服务，前端页面即可由同一个服务输出。

## 4. 技术栈清单

### 4.1 后端

- Python 3.13.1+
- FastAPI
- Uvicorn
- SQLAlchemy
- APScheduler
- Requests / HTTPX
- PyYAML
- Passlib/Bcrypt
- Playwright
- Selenium
- Redis（可选）

### 4.2 前端

- Vue 3
- Vite
- TypeScript
- Axios
- Arco Design Vue
- Ant Design Vue（项目已安装）

### 4.3 存储与运行环境

- SQLite：默认方案，最省事
- MySQL：Docker Compose 示例已内置
- PostgreSQL：配置样例给出了连接串格式，代码依赖中也包含驱动
- Redis：用于缓存、状态与异常统计增强
- Docker / Docker Compose：官方推荐部署方式之一

## 5. 目录结构与职责

### 5.1 根目录重要文件

- `main.py`：后端服务总入口
- `web.py`：FastAPI 应用入口
- `init_sys.py`：初始化数据库结构和默认管理员
- `config.example.yaml`：主配置模板
- `requirements.txt`：Python 依赖
- `README.zh-CN.md`：项目简介与基础说明

### 5.2 主要目录说明

#### `apis/`

接口层，直接对外暴露 REST API。按领域拆分：

- `auth.py`：登录、Token、二维码授权、AK/SK 管理
- `mps.py`：公众号管理、搜索、订阅添加、精选文章
- `article.py`：文章列表与文章相关操作
- `rss.py`：RSS/Feed 输出
- `message_task.py`：消息任务与任务执行
- `config_management.py`：配置管理
- `tags.py`：标签管理
- `export.py`：导入导出
- `cascade.py`：级联节点管理与调度接口

#### `core/`

项目核心能力层：

- `config.py`：读取配置、命令行参数、环境变量替换
- `db.py`：数据库连接、会话管理、表结构初始化与文章写入
- `auth.py`：JWT、AK/SK、权限校验
- `rss.py`：RSS XML 构建与缓存
- `task/task.py`：对 APScheduler 的封装
- `redis_client.py`：Redis 连接、状态与异常统计
- `notice/`：钉钉、企业微信、飞书、自定义通知
- `webhook/`：Webhook 数据拼装与发送
- `models/`：SQLAlchemy 模型定义
- `wx/`：微信公众号数据采集与聚合逻辑

#### `driver/`

微信相关驱动层，负责“怎么拿到微信数据”：

- `auth.py`：授权定时服务
- `wx.py`：扫码登录、Cookie/Token 提取、公众号信息抓取
- `playwright_driver.py`：Playwright 浏览器控制封装
- `wxarticle.py`：文章内容抓取
- `token.py` / `store.py` / `success.py`：登录态、状态管理
- `anti_crawler_*.js`：反爬规避脚本

#### `jobs/`

异步与定时任务层：

- `mps.py`：消息任务装载、定时采集、任务入队
- `webhook.py`：Webhook 推送执行
- `fetch_no_article.py`：补采未抓正文的文章
- `cascade_sync.py`：级联同步服务
- `cascade_task_dispatcher.py`：级联任务分发与子节点拉取
- `cascade_init.py`：级联初始化脚本

#### `web_ui/`

新的前端管理台：

- `src/views/`：页面视图
- `src/components/`：公共组件
- `src/api/`：前端 API 调用封装
- `src/router/`：路由与登录守卫
- `src/main.ts`：Vue 应用入口

#### `static/`

生产构建后的前端静态资源目录，后端直接挂载。

#### `public/templates/`

老版本模板页面资源。当前主流程已转向 Vue 前端，但该目录仍保留兼容性资源。

#### `compose/`

Docker Compose 部署文件：

- `docker-compose.dev.yaml`：开发态前后端分离 + 代理侧车
- `docker-compose.yaml`：较完整的容器化部署示例
- `singbox/`：代理侧车脚本

#### `docs/`

补充说明文档，重点包括：

- Access Key 文档
- Web UI 专项说明
- 级联系统设计与快速开始
- 缓存与环境异常统计说明

## 6. 认证、授权与安全模型

### 6.1 登录模型

后台管理登录默认使用 JWT。

- 登录接口：`POST /api/v1/wx/auth/login`
- 校验接口：`GET /api/v1/wx/auth/verify`
- 刷新接口：`POST /api/v1/wx/auth/refresh`

默认管理员由 `init_sys.py` 在初始化阶段创建：

- 用户名：`admin`
- 密码：`admin@123`

如果要修改，可通过环境变量覆盖：

```bash
export USERNAME=myadmin
export PASSWORD=mystrongpassword
```

### 6.2 Access Key 模型

系统支持 AK/SK 认证，适合程序化调用。

- Header 格式：`Authorization: AK-SK <access_key>:<secret_key>`
- AK 通过后台界面创建和管理
- Secret Key 只展示一次，服务端只保存哈希值

### 6.3 RSS 访问特性

`apis/rss.py` 中部分 RSS 路由默认未强制登录校验，方便 RSS 阅读器直接订阅。实际部署时建议：

- 放在内网或可信反向代理之后
- 配置独立域名
- 视场景增加网络访问限制

## 7. 配置体系说明

### 7.1 配置来源

项目主要通过 `config.yaml` 配置，模板文件是 `config.example.yaml`。配置值支持环境变量覆盖，格式如下：

```yaml
db: ${DB:-sqlite:///data/db.db}
```

也就是：

- 优先读取环境变量
- 没有环境变量时使用默认值

### 7.2 重点配置项

#### 服务与启动

- `server.enable_job`：是否启用定时任务
- `server.auto_reload`：是否自动重载
- `server.threads`：Uvicorn worker 数
- `port`：服务端口，默认 `8001`

#### 数据库与缓存

- `db`：数据库连接串
- `redis.url`：Redis 连接地址
- `cache.*`：缓存目录、是否启用、TTL

#### 微信采集

- `gather.content`：是否采集正文
- `gather.model`：采集模式，支持 `web/api/app`
- `gather.content_auto_check`：是否自动补采正文
- `gather.browser_type`：浏览器类型

#### RSS 输出

- `rss.base_url`：RSS 对外域名
- `rss.local`：是否输出本地文章地址
- `rss.title` / `rss.description` / `rss.cover`
- `rss.page_size`

#### 通知与 Webhook

- `notice.dingding`
- `notice.wechat`
- `notice.feishu`
- `notice.custom`
- `webhook.content_format`

#### 导出能力

- `export.pdf.enable`
- `export.pdf.dir`
- `export.markdown.enable`
- `export.markdown.dir`

#### 级联能力

- `cascade.enabled`
- `cascade.node_type`
- `cascade.parent_api_url`
- `cascade.api_key`
- `cascade.api_secret`
- `cascade.sync_interval`
- `cascade.heartbeat_interval`

### 7.3 最小本地配置示例

如果你只是先把项目跑起来，推荐从下面这个最小配置开始：

```yaml
app_name: we-mp-rss
server:
  enable_job: True
  auto_reload: False
  threads: 1
db: sqlite:///data/db.db
port: 8001
secret: we-mp-rss
gather:
  content: False
  model: web
  content_auto_check: True
  browser_type: firefox
rss:
  local: False
  page_size: 30
```

## 8. 部署与启动方式

### 8.1 方式一：快速 Docker 运行

适合先体验功能。

```bash
docker run -d \
  --name we-mp-rss \
  -p 8001:8001 \
  -v ./data:/app/data \
  ghcr.io/rachelos/we-mp-rss:latest
```

访问：

```text
http://<你的主机IP>:8001/
```

### 8.2 方式二：本地开发运行

#### 后端

```bash
pip install -r requirements.txt
cp config.example.yaml config.yaml
python main.py -job True -init True
```

#### 前端开发

```bash
cd web_ui
npm install
npm run dev
```

说明：

- 默认开发端口通常是 `5173`
- 前端通过 `src/api/http.ts` 访问后端 API
- 生产环境仍建议使用 `npm run build` 后由后端统一托管静态文件

### 8.3 方式三：Docker Compose 开发

适合前后端并行开发，并复用代理侧车：

```bash
docker compose -f compose/docker-compose.dev.yaml up -d --force-recreate
```

这个开发编排包含三个服务：

- `backend`：Python 后端
- `frontend`：Vite 开发服务器
- `singbox`：代理侧车

### 8.4 方式四：Docker Compose 完整部署

`compose/docker-compose.yaml` 给了一个包含 MySQL 和代理侧车的示例。适合需要较稳定部署的场景，但上线前建议自行调整：

- 数据库密码
- 挂载目录
- 反向代理
- HTTPS
- 日志与备份策略

## 9. 首次使用流程

### 9.1 初始化系统

首次启动建议执行：

```bash
python main.py -job True -init True
```

它会做两件关键事情：

- 创建数据库表
- 初始化默认管理员

### 9.2 登录后台

浏览器访问：

```text
http://localhost:8001/
```

使用默认管理员账号登录。

### 9.3 获取微信授权二维码

登录后进入相关授权流程，系统会调用：

- `GET /api/v1/wx/auth/qr/code`
- `GET /api/v1/wx/auth/qr/status`

底层由 `driver/wx.py` 调起 Playwright 浏览器，打开微信公众平台登录页，截取二维码并等待扫码完成。

### 9.4 添加公众号订阅

常见流程是：

1. 搜索公众号
2. 选择目标公众号
3. 保存订阅
4. 手动或定时拉取文章

对应的核心接口在 `apis/mps.py` 中。

### 9.5 查看文章与获取 RSS

订阅完成后，可以通过：

- 后台文章列表查看文章
- `/rss` 或 `/feed` 路径获取 RSS/Feed 输出

常见路径示例：

```text
GET /rss
GET /rss/{feed_id}
GET /rss/{feed_id}/fresh
GET /feed
```

实际可用路径以运行中的 Swagger 文档和前端页面生成为准。

### 9.6 创建消息任务

消息任务是这个项目的另一个核心能力。你可以为多个公众号配置定时任务，把新文章推送到：

- 钉钉
- 企业微信
- 飞书
- 自定义 Webhook

任务逻辑位于：

- 接口层：`apis/message_task.py`
- 调度层：`jobs/mps.py`
- 推送层：`jobs/webhook.py`

### 9.7 使用 Access Key 调 API

如果你需要脚本化访问后台数据，可以先在管理台中创建 AK，然后这样请求：

```bash
curl -H "Authorization: AK-SK your_access_key:your_secret_key" \
  http://localhost:8001/api/v1/wx/mps
```

## 10. API 与页面入口速查

### 10.1 文档地址

- Swagger UI：`/api/docs`
- ReDoc：`/api/redoc`
- OpenAPI：`/api/openapi.json`

### 10.2 API 前缀规则

后端统一 API 前缀来自 `core/base.py`：

```text
/api/v1/wx
```

前端 `web_ui/src/api/http.ts` 使用：

```text
/api/v1/ + /wx/...
```

所以大多数业务接口最终都是这种形式：

```text
/api/v1/wx/auth/login
/api/v1/wx/mps
/api/v1/wx/message_tasks
```

### 10.3 前端主要页面

根据 `web_ui/src/router/index.ts`，当前前端主要页面包括：

- `/login`：登录页
- `/`：文章列表首页
- `/add-subscription`：添加订阅
- `/wechat/mp`：公众号管理
- `/configs`：配置管理
- `/message-tasks`：消息任务
- `/tags`：标签管理
- `/access-keys`：Access Key 管理
- `/cascade`：级联管理
- `/env-exception`：环境异常统计

## 11. 级联系统说明

如果单节点性能不够，项目已经内置父子节点级联能力。

### 11.1 适用场景

- 多地区部署，降低采集延迟
- 多节点分摊抓取任务
- 中央节点统一管理公众号与任务

### 11.2 核心机制

- 父节点维护公众号、任务和子节点凭证
- 子节点按间隔向父节点同步配置并上报结果
- 任务分发由 `jobs/cascade_task_dispatcher.py` 管理
- 认证采用专用级联 AK/SK

### 11.3 推荐阅读

如果你要真正启用这块能力，优先阅读：

- `docs/CASCADE_QUICKSTART.md`
- `docs/CASCADE_GUIDE.md`
- `docs/CASCADE_README.md`

## 12. 开发者视角的架构理解

### 12.1 为什么这个项目要拆成 `apis/core/driver/jobs`

这种划分的本质是按职责边界拆：

- `apis/` 解决“对外暴露什么能力”
- `core/` 解决“系统内部通用能力怎么实现”
- `driver/` 解决“如何接入微信与浏览器”
- `jobs/` 解决“什么时候执行、异步怎么执行”

这个边界对后续维护很重要，因为：

- 接口改动不必直接污染驱动逻辑
- 采集能力可独立迭代
- 调度策略和业务接口可以分开维护
- 级联扩展不会强耦合到普通单机链路

### 12.2 当前架构的几个关键特点

#### 特点一：服务聚合入口明确

`main.py` 明确承担“应用装配器”的角色，所有后台能力最终在这里被组装并启动。

#### 特点二：前后端分离但交付一体

开发时前后端可分离，部署时又能由后端统一托管静态页面，降低运维复杂度。

#### 特点三：采集强依赖浏览器自动化

这个项目最特殊的技术点不在 CRUD，而在微信登录、文章抓取、反爬和登录态维持，所以 `driver/` 实际上是核心竞争力之一。

#### 特点四：任务系统是业务中枢

如果不启用任务调度，这个项目更像“手动管理器”；启用消息任务、Webhook 和自动补采后，它才成为完整的“自动分发平台”。

#### 特点五：级联能力让它具备横向扩展空间

不是所有 RSS 工具都有父子节点调度能力，这也是这个项目区别于轻量采集脚本的地方。

## 13. 常见问题与排障建议

### 13.1 服务能启动，但扫码授权失败

优先检查：

- 本机是否能访问微信公众平台
- 是否需要代理
- Playwright 浏览器依赖是否完整
- `gather.browser_type` 是否与实际环境匹配

如果在云主机部署，微信通常会对数据中心 IP 更敏感，优先考虑仓库里已有的 `singbox` 代理方案。

### 13.2 登录正常，但公众号文章抓不到

重点检查：

- 登录态是否过期
- Cookie/Token 是否已保存
- 当前账号是否有目标公众号权限
- 是否被微信风控
- 任务是否真的执行到抓取链路

### 13.3 RSS 正常，但 Webhook 没推送

重点检查：

- 任务是否启用
- Cron 表达式是否正确
- `server.enable_job` 是否为 `True`
- Webhook 地址、Headers、Cookies 是否配置完整
- 外部目标系统是否返回错误

### 13.4 环境异常统计没有数据

该功能依赖 Redis 增强能力。若未配置 Redis，相关统计能力可能不可用或信息不完整。

### 13.5 前端页面空白或资源 404

重点检查：

- 是否已经执行 `npm run build`
- `static/index.html` 是否存在
- `web.py` 是否正确挂载 `static/` 与 `/assets`
- 若走开发模式，Vite 代理是否正确指向后端

## 14. 建议的使用顺序

如果你是第一次接手这个项目，建议按这个顺序推进：

1. 先用 SQLite 在本地跑通
2. 登录后台并验证默认管理员
3. 完成一次微信扫码授权
4. 添加一个公众号并手动抓一轮文章
5. 验证 RSS 链接可被阅读器订阅
6. 创建一个简单消息任务，验证 Webhook
7. 再决定是否启用 Redis、MySQL、级联能力

## 15. 二次开发建议

### 15.1 看代码优先级

建议按这个顺序读源码：

1. `main.py`
2. `web.py`
3. `core/config.py`
4. `core/db.py`
5. `apis/auth.py`
6. `apis/mps.py`
7. `apis/rss.py`
8. `jobs/mps.py`
9. `driver/wx.py`
10. `web_ui/src/router/index.ts`

### 15.2 改动边界建议

- 改 API 行为，优先从 `apis/` 入手
- 改登录/权限，优先看 `core/auth.py`
- 改 RSS 格式，优先看 `core/rss.py`
- 改抓取和扫码，优先看 `driver/`
- 改调度和消息推送，优先看 `jobs/`
- 改后台页面，优先看 `web_ui/src/views/`

### 15.3 测试建议

这个仓库自动化测试覆盖不高，实际开发时建议至少做三类验证：

- 单点接口验证
- 启动服务后的手工冒烟
- 前端 `npm run build`

已存在的一个后端测试示例位于：

```text
core/lax/test_template_parser.py
```

## 16. 总结

从技术架构上看，WeRSS 不是单纯的 FastAPI 管理台，而是一个围绕“微信公众号抓取与内容分发”构建的完整系统：

- Web UI 负责管理体验
- FastAPI 负责对外服务与统一入口
- `core/` 提供配置、认证、数据库、RSS、调度等基础设施
- `driver/` 负责最关键的微信自动化能力
- `jobs/` 负责把抓取与推送真正跑起来
- `compose/` 和 `docs/` 提供部署与扩展支撑

如果你的目标是“先理解再接手”，先跑单机 SQLite 版本最合适；如果你的目标是“稳定使用”，重点应放在代理、登录态维持、消息任务和备份策略；如果你的目标是“扩展采集能力”，则应继续深入级联系统文档与 `jobs/cascade_*` 实现。
