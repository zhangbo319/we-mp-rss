# 开发计划-服务假死排查修复
创建时间：2026-03-28
当前状态：已完成
执行目标：定位本地 Docker 部署下服务网页假死的根因，并完成最小修复与回归验证。
步骤：[x] 复现服务假死并收集线程栈证据  [x] 补充异步抓取阻塞回归测试  [x] 修复文章抓取异步执行逻辑  [x] 重启并验证 Docker 服务链路  [x] 回填结果
改动明细：
- 排查确认 `/api/v1/wx/mps/by_article` 调用链会进入 `driver/wxarticle.py` 的 `async_get_article_content`。
- 使用 `py-spy dump --pid 1` 抓取容器内主线程栈，确认 Uvicorn 主线程阻塞在 `ThreadPoolExecutor.__exit__`，导致服务假死。
- 新增 `driver/test_wxarticle_async.py`，验证文章抓取异步封装不会阻塞事件循环。
- 修复 `driver/wxarticle.py` 中的 `async_get_article_content`，改为 `asyncio.to_thread(...)` 执行阻塞型 Playwright 抓取逻辑，避免主线程等待线程池退出。
- 调整 `driver/wx_api.py` 中登录状态轮询日志，将裸 `print(data)` 收敛为 `logger.debug(...)`，减少故障期间日志噪音。
校验结果：
- `curl -m 8 http://localhost:8001/` 在故障时超时，无响应内容。
- `docker exec we-mp-rss-backend-host-mysql py-spy dump --pid 1` 显示阻塞栈位于 `apis/mps.py -> driver/wxarticle.py:async_get_article_content`。
- `docker exec we-mp-rss-backend-host-mysql python -m unittest driver.test_wxarticle_async -v` 先失败后通过，确认回归测试覆盖到事件循环阻塞问题。
- `docker exec we-mp-rss-backend-host-mysql python -m unittest driver.test_wx_lock -v` 通过，确认此前二维码锁修复未被回退。
- `docker compose -f compose/docker-compose.host-mysql.yaml restart backend` 完成重启，`docker compose ... ps` 显示容器状态为 `Up`。
- `curl --max-time 10 http://localhost:8001/` 返回前端 HTML。
- `curl -I --max-time 10 http://localhost:8001/api/docs` 返回 `200 OK`。
- `curl -X POST http://localhost:8001/api/v1/wx/auth/login ...` 成功返回 token。
- `POST /api/v1/wx/mps/by_article?...` 日志返回 `200 OK`，修复后未再出现整站假死；当前该接口仍会因容器内未安装 Playwright 浏览器而返回 `fetch_error`，属独立运行环境问题。
